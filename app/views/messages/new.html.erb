<!-- Validation failure messages -->
<% if @message.errors.any? %>
  <% @message.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Message</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_message" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for sender_id -->
      <div class="form-group">
        <label for="sender_id" class="control-label">
          Sender
        </label>

        <input type="text" id="sender_id" name="sender_id" placeholder="Sender" class="form-control" value="<%= @message.sender_id %>">
      </div>

      <!-- Label and input for reciever_id -->
      <div class="form-group">
        <label for="reciever_id" class="control-label">
          Reciever
        </label>

        <input type="text" id="reciever_id" name="reciever_id" placeholder="Reciever" class="form-control" value="<%= @message.reciever_id %>">
      </div>

      <!-- Label and input for msg_content -->
      <div class="form-group">
        <label for="msg_content" class="control-label">
          Msg content
        </label>

        <textarea id="msg_content" name="msg_content" placeholder="Msg content" class="form-control" rows="3"><%= @message.msg_content %></textarea>
      </div>

      <button class="btn btn-success">
        Create Message
      </button>
      or
      <a href="/messages">Cancel</a>
    </form>
  </div>
</div>
